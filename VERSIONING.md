# Mac灵动岛版本与发布规则

当前版本为 `1.4.3`，版本号遵循 `主版本.功能版本.修订版本`。

- 普通更新：最后一位未到 9 时加一；到 9 时自动进位，例如 `1.0.8` → `1.0.9`、`1.0.9` → `1.1.0`、`1.9.9` → `2.0.0`。
- 较大功能更新：增加中间一位并把最后一位归零，例如 `1.0.9` → `1.1.0`。
- 下一次较大功能更新继续为 `1.5.0`、`1.6.0`。
- 不要为单纯的代码保存或尚未发布的中间修改增加版本号；每次准备发布时增加一次。

普通更新：

```bash
python3 scripts/bump_version.py patch
```

较大功能更新：

```bash
python3 scripts/bump_version.py minor
```

版本修改与功能代码一起提交并推送到 `main` 后，GitHub Actions 会读取 Xcode 项目中的版本号。如果对应的 `vX.Y.Z` 标签尚不存在，就自动创建标签和 GitHub Release。Release 默认包含 GitHub 生成的源码压缩包；DMG 安装包需要另行完成签名和构建配置后才能自动附加。
